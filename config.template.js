/**
 * config.js is generated automatically at deploy time by build.js,
 * which reads values from environment variables (see .env.example).
 *
 * Nothing in this file is a secret — it's the public Calendly booking
 * link for the "MjengoAI Pro Demo" event type. Do not put API keys,
 * OAuth tokens, or other credentials here or anywhere in frontend code.
 */
window.MJENGOAI_CONFIG = {
  // Public Calendly scheduling link for the "MjengoAI Pro Demo" event type.
  // Set via the CALENDLY_URL environment variable.
  calendlyUrl: "__CALENDLY_URL__"
};
